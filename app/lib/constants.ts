/**
 * The default number of items to take in a Prisma paginated query.
 */
export const DEFAULT_TAKE = 10;

/**
 * Our application's maximum-supported review rating.
 */
export const RATING_MAX = 5;
